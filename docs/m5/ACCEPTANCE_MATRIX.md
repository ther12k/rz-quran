# Acceptance Reconciliation Matrix (v1.1 review pass)

Generated 2026-09-05 per `RZ-Quran-Kids-All-in-One-v1.1/acceptance-review/03_RECONCILIATION_AGENT_PROMPT.md`.
This document distinguishes **implemented**, **tested**, **externally approved**, and **remaining gap**
for all 74 original backlog tasks. Proposed GitHub issue changes are proposals only (see §Proposed issue changes).

## Baseline

| Field | Actual value |
| --- | --- |
| Repository / inspected commit | ther12k/rz-quran @ `669c5e3` + uncommitted reconciliation delta (see execution log) |
| Baseline handoff | RZ-Quran-Kids-Handoff-v1.0 (preserved byte-for-byte in v1.1 package) |
| Issue-state snapshot | 2026-09-05: 67 closed / 7 open (T063–T068, T070 open; T069 closed) |
| Authorized scope changes superseding baseline | **None found** — no recorded product-owner decision relaxes any original criterion |
| Auditor / environment | Implementation agent, local dev stack (Bun 1.4.0, PostgreSQL 16 docker, Chromium 1.63) |

## Acceptance matrix

| Task | Original criterion (abridged; full text in tasks/BACKLOG.md) | Implementation evidence | Executed test evidence | External approval evidence | Remaining gap | Recommended status |
| --- | --- | --- | --- | --- | --- | --- |
| **T001** Inspect repository and preserve existing work | Document current files, scripts, stack and unrelated changes. / Identify the children’s app boundary; do not overwrite an existing Fiqh chatbot module. | docs/m0/01_REPOSITORY_ASSESSMENT.md documents stack/files; Fiqh project untouched (separate tree) | n/a (documentation) | n/a | none | Met — keep closed |
| **T002** Verify dependency and runtime compatibility | Record exact React/Vite/Bun/Elysia/auth/ORM versions actually tested. / Build a minimal Vite frontend and Elysia/auth integration; commit a pinned lockfile. | docs/m0/02_VERSION_COMPATIBILITY_RECORD.md; pinned lockfile | CI installs lockfile reproducibly and runs full pipeline | n/a | none | Met — keep closed |
| **T003** Assign eligibility, consent and retention decisions | Assign owners for D02/D03/D09/D10 and list unresolved market/risk questions. / Document that production child enrollment is blocked pending approved policy. | docs/m0/03_CONSENT_DECISIONS.md (owners D02/D03/D09/D10, open questions); production enrollment fail-closed in env.ts/consentPolicy | unit/env.test.ts production policy guards; e2e-slice ELIGIBILITY_BLOCKED | Policy decisions themselves pending (tracked T065) | none for this task's record-keeping scope | Met — keep closed |
| **T004** Inventory canonical text, audio and curriculum dependencies | List candidate source releases and missing rights evidence separately per asset type. / Assign qualified content author/reviewer roles; do not assert approval. | docs/m0/04_CONTENT_INVENTORY.md separates sources vs missing rights per asset type | n/a (inventory) | Rights evidence pending (tracked T063) | none for inventory scope | Met — keep closed |
| **T005** Implement tokens and accessible UI primitives | Translate design tokens into actual typography, spacing, focus and button components. / Check intended color pairs and 48-pixel touch targets without using screenshot backgrounds. | apps/web/src/styles.css tokens; components/ui.tsx primitives (focus, 48px touch) | tests/e2e/m4-accessibility.spec.ts | n/a | none | Met — keep closed |
| **T006** Create workspace scripts and CI | Provide lint, typecheck, unit, integration, E2E, contract and build scripts. / CI installs the lockfile reproducibly and runs against a disposable database. | root package.json scripts (lint/typecheck/unit/integration/e2e/contracts/build); .github/workflows/ci.yml | CI green on GitHub runs (postgres:16 service) | n/a | none | Met — keep closed |
| **T007** Integrate schemas and public DTO contracts | Validate all bundled positive and negative examples. / Separate staff authoring payloads from child/public serialization, especially answer keys. | packages/contracts zod DTOs + JSON schemas; staff authoring payloads separated from public serialization | tests/contracts/examples.test.ts validates all bundled positive+negative examples | n/a | none | Met — keep closed |
| **T008** Implement checked database migrations | Generate auth schema and domain migrations with correct foreign keys. / Test clean apply and ownership/completion uniqueness constraints on PostgreSQL. | packages/database/migrations 0000–0003 (auth + domain, composite FKs) | clean apply per disposable test DB in every integration run; uniqueness constraints exercised in T022/T073 tests | n/a | none | Met — keep closed |
| **T009** Create guarded synthetic demo fixtures | Seed only clearly marked non-production data and missing-audio states. / Production startup/publication refuses demo flags, fixtures and test consent methods. | packages/database/src/seed-demo.ts guarded by APP_ENV+DEMO_MODE; missing-audio states honest (audioAssetId null) | unit/env.test.ts refuses production/staging; preflight demo-data gate | n/a | none | Met — keep closed |
| **T010** Review trust boundaries and threat cases | Map parent/child/staff capabilities and mounted-auth bypass risks. / Write negative-test inventory for ownership, answer leakage, replay, imports and exports. | docs/m0/05_THREAT_MODEL.md + negative-test inventory | tests/security/* implements the inventory | n/a | none | Met — keep closed |
| **T011** Integrate adult authentication | Use the maintained auth library for identity/password/session flows. / Keep secrets server-side; verify auth routes work behind the same-origin proxy. | Better Auth integration (auth.ts) drizzle adapter; same-origin enforcement (APP_ORIGIN) | e2e-slice sign-up/in/out journey | n/a | none | Met — keep closed |
| **T012** Implement email verification and recovery | Unverified account cannot create a real child profile. / Recovery/verification expiry and resend limits have tested safe UI states. |  Stateless-JWT email verification wired + verification pending UI state | sign-up → verify → sign-in tested (e2e-slice setup helper) | n/a | PASSWORD RECOVERY NOT IMPLEMENTED: gate.tsx references 'tautan pemulihan di halaman masuk' but no forgot/reset flow exists in API or web; expiry/resend-limit criteria untested | Partially met — propose REOPEN (engineering remainder: implementable locally, no external blocker) |
| **T013** Enforce server-side parent gate | Password reauthentication creates a five-minute server gate. / Entering child mode, expiry and identity changes revoke adult privileges. | POST /v1/parent/gate password reauth → 5-min adult_gate_until; child-mode entry clears gate; S15 return unlock | isolation.test.ts expired-gate case; T055 rate-limit tests; review-session regression (failures-only budget) 8× unlock test | n/a | none | Met — keep closed |
| **T014** Guard the mounted authentication routes | Define a child-mode allowlist for safe auth-library routes. / Direct account/session/email mutation attempts cannot bypass the app parent gate. | child-mode auth allowlist in modules/identity.ts (.all catch-all) | isolation.test.ts: parent/account mutation blocked in child mode with valid adult cookie | n/a | none | Met — keep closed |
| **T015** Implement consent and eligibility state machine | Versioned family/child consent actions use server-validated assurance references. / Unconfigured production policy blocks enrollment; demo assurance is local/test only. | consent state machine, versioned notices, server-validated assurance; production unconfigured → fails closed | e2e-slice: wrong assurance token 403 ELIGIBILITY_BLOCKED; demo grant 201 | Approved policy/assurance tokens for production pending (T065) | none for state machine scope | Met — keep closed |
| **T016** Implement parent-owned child profiles | Nickname/avatar/age-band inputs validate and exclude unnecessary personal fields. / Three-profile limit is race-safe and all CRUD operations are owner-scoped. | children CRUD owner-scoped; nickname/avatar/age-band only; 3-profile limit via FOR UPDATE lock | isolation.test.py cross-parent denials + 4th-profile rejection | n/a | none | Met — keep closed |
| **T017** Implement active child and mode switching | Active child is stored in server session context and entering child mode clears the adult gate. / Switch/logout clears audio, query caches and pending personal UI state. | active child + mode in server session (session_controls); entering child mode clears gate; web clears caches/audio on switch | e2e journey mode switches | n/a | none | Met — keep closed |
| **T018** Create scoped repositories and authorization tests | Every private lookup receives authenticated owner context. / Cross-parent read/write/select/delete attempts have consistent denial responses. | all parent lookups scoped by ctx.parent.id | isolation.test.ts id-substitution matrix | n/a | none | Met — keep closed |
| **T019** Serve one demo lesson through safe serialization | Local-only published-demo view returns only permitted child fields. / No correct options, internal notes, reviewer identities or provider secrets escape. | lesson GET returns permitted child fields only | tests/security/response-shapes.test.ts (new): forbidden-field scan incl. answer keys | n/a | none | Met — keep closed |
| **T020** Create pinned learning sessions | Sessions pin a lesson version and derive child identity from the server context. / Concurrent session creation respects the one-writable-session rule and idempotency. | sessions pin version_id, child from server context; one-writable-session partial unique index | isolation.test.ts concurrent creation; T073 parallel tests | n/a | none | Met — keep closed |
| **T021** Accept ordered idempotent learning events | Atomic batches validate session/unit ownership and contiguous sequences. / Identical retries reuse results; conflicting event IDs or sequence gaps fail safely. | ordered idempotent events (atomic batch, contiguous sequences, Idempotency-Key + payload hash) | IDEMPOTENCY_CONFLICT + EVENT_ID_CONFLICT tests (isolation.test.ts) | n/a | none | Met — keep closed |
| **T022** Finish sessions and award first-completion stars | Completion requires all server-defined units. / Concurrent/repeated finish returns one completion and at most one logical-lesson star. | finish requires all required units; single star via first-completion uniqueness | T073 parallel finish awards exactly one star | n/a | none | Met — keep closed |
| **T023** Build usable child home and demo activity | At 390 px a real continue/start action is visible without squeezing cards. / First-use, resumed, no-content and pending-save states use actual data. | child home with real continue/start card, skeletons for CLS, empty states | m1/m2 e2e; CLS budget test 0.000 | n/a | none | Met — keep closed |
| **T024** Build a real gated parent progress slice | Read real scoped lesson completion and active-time state from database. / Empty data is labeled honestly and reports require a live parent gate. | parent progress from DB projections, gate-required | e2e honest zero states test | n/a | none | Met — keep closed |
| **T025** Prove the end-to-end safe slice | Run adult setup → child lesson → finish → parent report on a real local database. / Exercise alternate-parent IDs, direct parent URLs and replayed completion requests. | full journey on real local PostgreSQL | tests/api/e2e-slice.test.ts incl. alternate-parent/replay attacks | n/a | none | Met — keep closed |
| **T026** Implement catalog search and filters | Only eligible published content is searchable; no web search dependency. / Debounced search cancels stale requests and has empty/error/no-result states. | catalog search/type/stage filters on allPublished; debounced client | m2 test filter matrix | n/a | none | Met — keep closed |
| **T027** Implement versioned stages and prerequisites | Curriculum graph validation rejects cycles and broken logical lesson references. / Locked states offer a valid next step and stable reporting denominator. | curriculum DAG (stages, prerequisites, curriculum_releases) cycle-safe; locked cards show next step | m2 catalog prerequisite assertions | n/a | none | Met — keep closed |
| **T028** Build the single audio controller | Play/pause/loading/buffering/ended/unavailable states are deterministic. / Route switch, profile change and stale media callbacks cannot play/advance the wrong lesson. | audio/controller.ts singleton with generation tokens; route switch stops playback | manual + player integration; stale-token cancellation exercised in m2 flows | n/a | no licensed audio yet (T063) so end-to-end audio paths run against missing-audio states | Met — keep closed |
| **T029** Implement authorized media delivery | Only approved assets entitled by published/current session content receive short-lived URLs. / Private/quarantine/withdrawn assets and arbitrary user-submitted URLs are denied. | /media/:id/playback requires child session + asset verified | m3 (new): recalled asset → 503 MEDIA_UNAVAILABLE | n/a | STREAM ROUTE MISSING: playback_url targets /api/v1/media/stream/:id?token=… which has no handler; token is a bare UUID with no expiry signature. Mitigated: zero licensed audio exists | Partially met — propose REOPEN (engineering remainder: implementable locally) |
| **T030** Implement listen-and-imitate steps | Requested audio and one/three repeats work without microphone permission. / Practice acknowledgment is labeled as practice, not pronunciation assessment. | listen-imitate steps 1×/3× repeat; explicit 'no recording' notices | m2 player tests; outbound-behavior e2e proves no getUserMedia | n/a | none | Met — keep closed |
| **T031** Implement short-surah and ayah practice UI | Render source-keyed Arabic, attribution and last accepted unit accurately with reviewed staging data. / Missing media/content never falls back to generated recitation or fabricated text. | surah player renders Tanzil canonical text keyed by verse_key, staged via reviewed seed | m2 asserts exact verse strings; t058 spec | Curriculum approval of pilot content (T064) | none for rendering scope | Met — keep closed |
| **T032** Assemble private question sessions and public choices | Exactly five eligible reviewed questions/rounds are selected for configured pilot sets. / Public question data omits the correct option and records presentation order server-side. | 5-question quiz / 5-round game from published pilot lesson; options carry no answer flags | response-shapes scan (no is_correct/correct_answer); m2 quiz flow | n/a | none | Met — keep closed |
| **T033** Implement first-response scoring and retries | Selected option membership and question/session/version mapping validate server-side. / Retries preserve first-answer correctness and cannot duplicate reward/sequence effects. | server-side membership validation; first-answer preserved on retries | m2 answer flow; first_answers table | n/a | none | Met — keep closed |
| **T034** Build quiz question and completion states | Five-question flow supports keyboard, large choices, feedback and retry. / No timer pressure, hidden answer-key data or ungrounded performance claims. | quiz UI keyboard/feedback/retry; no timer pressure | m2 e2e + a11y spec keyboard pass | n/a | none | Met — keep closed |
| **T035** Build five-round sound-matching game | Tap and keyboard interaction work without drag; optional drag shares selection logic. / Playing/feedback/completed views are mutually coherent and use real reward results. | 5-round sound game, tap/keyboard, no drag required | m2 game flow | n/a | none | Met — keep closed |
| **T036** Implement resume and serial mutation queue | Answers, acknowledgments and heartbeats share ordered per-session submission. / Reconnect replays original IDs; replaced/expired/recalled sessions cannot sync into another session. | serial per-session submission queue in player; sequence-conflict recovery via /learning/current resync | EVENT_SEQUENCE_CONFLICT retry tested (isolation) | n/a | none | Met — keep closed |
| **T037** Add parent memorization observations | Only the owning gated parent can append an observation for a surah. / Latest observation and history stay separate from automatic practice completion. | parent assessments append-only, gate+ownership required | m2 assessment tests | n/a | none | Met — keep closed |
| **T038** Implement active-time buckets and parent reports | Server caps heartbeats and prevents multi-tab duration duplication. / Weekly totals, quiz denominators, timezone labels and no-data states agree with stored records. | heartbeat caps (15s), active_ms buckets dedupe multi-tab, timezone snapshots in reports | m2/reporting tests | n/a | none | Met — keep closed |
| **T039** Build child achievements | Stars come from unique logical-lesson completion records. / Badges describe practice and contain no ranking, spending, streak loss or automatic hafal claim. | stars from unique first-completion; badges practice-only, no rankings/streaks | m2 achievements; T073 one-star proof | n/a | none | Met — keep closed |
| **T040** Implement soft session goals and comfort settings | Parent-selected goal offers a calm break without coercive lockout. / Quiet and reduced-motion settings persist with correct child scope and audio semantics. | comfort settings (quiet/reduced-motion) persisted per child; calm break goal, no lockout | m2 settings tests | n/a | none | Met — keep closed |
| **T041** Provision staff capabilities and MFA gates | Admin routes require active provisioned role and current MFA evidence. / Content staff cannot read child reports or impersonate families. | staff_members capabilities + requireStaffCapability on all /admin routes; staff cannot access parent-scoped routes | m3 role enforcement flows | n/a | MFA GATE NOT IMPLEMENTED: criterion 'current MFA evidence' has no implementation (no TOTP/second factor for staff) | Partially met — propose REOPEN (engineering remainder: implementable locally) |
| **T042** Implement source/rights registry | Store source versions, attribution, evidence, allowed use and reviewer decisions. / Pending/denied/revoked rights block content publication. | sources registry with versions, attribution, evidence, permitted uses, rights decisions (pending default) | m3: pending/denied blocks publication path | Verified rights documents (T063) | none for registry scope | Met — keep closed |
| **T043** Implement quarantined media upload and verification | Signed uploads are constrained; actual bytes/MIME/size/checksum/decoding are validated. / Unverified files cannot be streamed to child sessions. | asset upload → quarantine → worker verification (checksum/mime/size) before 'verified' | m3 quarantine + worker test | n/a | signed-upload URL issuance against real S3 not exercised locally (no storage backend configured) | Met at local scope — keep closed; production storage integration re-verified at launch |
| **T044** Build allowlisted source import adapters | Imports use registered source/object/adapter IDs, not unrestricted URL fetching. / Provider credentials/environment permissions are server-only and failures are redacted. | canonical import via local seed scripts against registered sources; no unrestricted URL fetching anywhere in the API | m2 canonical text assertions (exact Tanzil strings) | Source rights (T063) | none | Met — keep closed |
| **T045** Validate canonical text and audio mappings | Approved source metadata validates verse keys/counts and immutable hashes. / Every pilot segment has recorded human text/audio alignment checks before production. | release hashing (canonical JSON + SHA-256), verse-key/count validation in contracts + m2 exact-text tests | contracts examples + m2 assertions | none | HUMAN ALIGNMENT NOT RECORDED: 'every pilot segment has recorded human text/audio alignment checks' has no evidence; audio does not exist yet (licensed audio pending T063), so audio alignment is currently impossible; text alignment sign-off must be an explicit T064 sub-item | Partially met — propose REOPEN (blocked-external; explicit mapping to T064 recorded) |
| **T046** Build structured lesson editor and staff preview | Editor validates units/options/references and shows precise blocking errors. / Draft preview stays staff-only; changes after review require a new candidate/review. | structured lesson authoring validation with precise errors; staff-only preview | m3 draft→submit flow tests | n/a | none | Met — keep closed |
| **T047** Build curriculum release editing and review | Validate order, stage and prerequisite DAG with stable denominator. / Distinct reviewer and immutable release hash are required before publishing. | curriculum release editing with DAG validation + immutable release hash + distinct reviewer | m3 curriculum release test | n/a | none | Met — keep closed |
| **T048** Implement review and publish transactions | Author cannot approve their own candidate and published payloads cannot be edited in place. / Rights, hash, audio and question-count failures prevent publication atomically. | publish transaction: distinct reviewer required (self-review denied + audited), immutable versions | m3 self-review denial test | n/a | none | Met — keep closed |
| **T049** Implement recall and content-status invalidation | Recalled content disappears from new catalog/session/media access. / In-flight sessions stop on the next check with safe return path and historical progress retained. | recall: version+lesson status flip, active sessions invalidated (410 on next write), catalog hides, lesson GET 404, new session 404, media authorization blocked (shared-asset aware) | m3 (extended today): all five surfaces asserted incl. media cascade | n/a | already-delivered bytes cannot be retracted (documented runbook limit); Cache-Control no-store set on media auth | Met — keep closed (strengthened during reconciliation) |
| **T050** Build parent content reporting and triage | Reason codes and optional short notes are owner-gated and private. / No child audio/photo attachments or public report publication. | owner-gated content reports with reason codes, private triage | m3 report flow | n/a | none | Met — keep closed |
| **T051** Implement redacted audit trail | Privileged actions include actor, object, request ID, outcome and immutable time. / Audit metadata excludes raw child fields, passwords, signed URLs and support text. | audit_events immutable trail (actor/object/request_id/outcome/time); no raw child fields | m3 audit assertions; response-shapes scan | n/a | none | Met — keep closed |
| **T052** Implement withdrawal and reconsent enforcement | Family withdrawal blocks all profiles; child withdrawal blocks that profile immediately. / New consent requires approved current flow and revoked sessions cannot continue writing. | withdrawal family/child scoped immediate enforcement; reconsent requires current approved flow | m4 withdrawal tests | n/a | none | Met — keep closed |
| **T053** Implement private export jobs | Owner-gated export includes only permitted family data with schema notes. / Downloads expire, enforce ownership and omit secrets/answer keys/internal evidence. | export jobs owner-scoped, schema-noted, expiring downloads | m4 export job test | n/a | none | Met — keep closed |
| **T054** Implement deletion and backup suppression | Deletion immediately revokes access and idempotent jobs remove active data/objects. / A minimized suppression ledger prevents deleted profiles returning after restore. | deletion revokes access, idempotent jobs, minimized suppression ledger | m4 deletion test + restore drills (resurrection replay proven) | n/a | off-site ledger copy needs real infra (tracked T069) | Met at local scope — keep closed |
| **T055** Enforce request security and distributed limits | CSRF/origin/cookie/CSP/body-size controls are verified on real routes. / Rate limits remain effective across configured API replicas and do not expose account existence. | origin checks (APP_ORIGIN), Better Auth cookie defaults, fixed-window distributed rate limits on gate + events | T055 rate-limit tests (failures-only budget + toggle lockout); origin mismatch rejected in tests | n/a | EXPLICIT CSP HEADER AND REQUEST BODY-SIZE LIMIT NOT IMPLEMENTED/VERIFIED — criterion partially unmet (implementable locally) | Partially met — propose REOPEN (small engineering remainder) |
| **T056** Implement installable shell and explicit offline states | Service worker caches only static shell/assets, not private API/content/media. / Cold offline, in-memory queue limit and reload loss are accurately explained. | service worker caches static shell only; explicit offline/queue-loss states in UI | sw registration prod-only; manual local offline verification; honest queue-loss notices | n/a | no automated offline-mode e2e test (manual evidence only) | Met at local scope — keep closed; automated offline test noted as follow-up |
| **T057** Verify responsive and accessible flows | Capture 320/390/768/1024/1440 layouts and keyboard/200%-zoom checks. / Check contrast, tap alternatives, focus, screen-reader feedback and chart table equivalents. | 320/390/768/1024/1440 layouts captured; keyboard/200% zoom/contrast/focus checks | evidence/screenshots (100 frames) + m4-accessibility spec | n/a | WebKit/Safari not in the automated matrix (Chromium only) — boundary noted | Met at local scope — keep closed with boundary note |
| **T058** Review actual Arabic rendering | Use reviewed source text with marks on mobile Safari/Chrome and desktop browsers. / No clipped marks, accidental translation, reversed strings or screenshot-sourced glyph content. | Arabic rendered from reviewed Tanzil text with marks; RTL isolate, no letter-spacing, Noto Naskh | t058 spec reads verses from DB and verifies rendering at 100%/200% zoom | n/a | mobile Safari manual check pending real devices (pilot, T067) | Met at local scope — keep closed with boundary note |
| **T059** Measure performance budgets | Record test hardware/network and actual LCP/INP/CLS/API latency measurements. / Investigate oversized bundles, media latency and unnecessary rerenders; do not report unmeasured targets as results. | PERFORMANCE_REPORT with full measurement conditions (added today: dataset scale, cache state, run structure, lab-scope label) | LCP 1656/INP 41/CLS 0 under CPU4× slow-4G; API p95 175ms @50 conc; raw JSON artifacts | n/a | production CDN/field measurement at launch (tracked T069); no RUM for children (by design) | Met at lab scope — keep closed |
| **T060** Audit logs, frontend bundles and outbound calls | No provider secret, password, answer key or private field leaks to child/public responses/logs. / No ads, tracking pixels, session replay or microphone/camera calls exist in MVP. | static: source+bundle leak scans; runtime (added today): response-shape security tests + first-party-only outbound e2e; Google Fonts removed → self-hosted OFL fonts | leak-scans (15), response-shapes (2), outbound-behavior (1) all green | n/a | none for exercised flows | Met — keep closed (strengthened during reconciliation) |
| **T061** Implement deployment and idempotent workers | Document real environment setup, health/readiness, job leases/retries and rollback. / Production startup validates secrets/policy flags and forbids fixture mode. | health/readiness routes; worker FOR UPDATE SKIP LOCKED leases; production startup fail-closed (secrets/policy/fixtures); rollback documented in runbook + drill | worker job processing tests; env guards; restore drill | Real deployment environment (T069/D09) | production-scale execution | Met at local scope — keep closed; production execution tracked T069 |
| **T062** Exercise restore and deletion replay | Restore an isolated backup and measure actual RPO/RTO outcomes. / Reapply suppression ledger and verify source pointers/ownership before declaring recovery. | restore drill with resurrection proof, independent single-table ledger artifact restored standalone, RPO anchor, timer boundaries, honest limits | latest drill PASS (all 6 verification checks) | Off-site artifact storage needs real infra | production-scale end-to-end service drill (T069) | Met at local scope — keep closed; production drill tracked T069 |
| **T063** Approve production content rights | Obtain/document actual source, recording, streaming and distribution permissions. / Record approved attribution and any caching/offline restrictions; unresolved rights remain blocked. | registry + blocking ready (rights_status gates publication) | m3 rights-gating tests | REQUIRED: signed source/recording/streaming/distribution permissions | actual rights evidence from owners | External approval — keep open |
| **T064** Approve pilot curriculum and all mappings | Human reviewer distinct from author checks text/audio/questions and stage sequence. / Evidence references exact release hashes, not generic approval of the app. | two-person review infrastructure + release hashes ready for reviewer evidence | m3 review flow | REQUIRED: qualified reviewer sign-off per release hash (now explicitly includes T045 human text/audio alignment per pilot segment) | reviewer evidence | External approval — keep open |
| **T065** Approve market, eligibility, consent and retention policy | Resolve applicable Indonesian 2026 framework and any other launch markets. / Record signed policy/assurance/processor/notice decisions before enabling child enrollment. | policy surfaces implemented (notices, consent records, DSR flows) | m4 privacy tests | REQUIRED: signed market/eligibility/consent/retention decisions | policy decisions | External approval — keep open |
| **T066** Finalize production artwork and missing screen states | Use licensed/original assets with rights evidence, not whole-screen screenshot slicing. / Review onboarding/gate/ayah/privacy/admin states and responsive screenshots not supplied as original concepts. | flat in-house UI ships with no unlicensed art; asset pipeline + manifest ready | mockup comparison record (evidence/mockup-compare/COMPARISON.md) | REQUIRED: licensed/original production artwork + design acceptance | artwork rights + integration pass | Mixed gate — keep open |
| **T067** Run supervised usability pilot | Use the approved participant/consent protocol and record actual task observations. / Assess child navigation and parent interpretation of practice versus memorization without overstating efficacy. | app runnable for supervised sessions; pilot environment available locally | demo environment verified today | REQUIRED: approved protocol + real participants + recorded observations | pilot execution | External — keep open |
| **T068** Resolve pilot defects and run complete regression | Attach real automated/manual results for the full requirement matrix. / No unresolved critical/high security issue or known critical content defect remains. | regression harness ready (full suite green) | typecheck + 13 integration + 17 security + e2e suites | Depends on T067 findings | pilot defect remediation + full-matrix regression run | Blocked engineering/QA — keep open (reclassified: NOT a human-only approval) |
| **T069** Run production preflight and incident drills | Verify deployment, recovery, source recall, export/deletion and incident contacts. / Confirm no demo records, fake statistics or unapproved source releases are published. | preflight tooling complete + fail-closed; itemizes PENDING-HUMAN gates | verified run documented in issue #73 | Prerequisites T063/T064/T065/T068 + release environment | OPERATIONAL EXECUTION: run preflight + incident drills against the actual release environment and record outcomes | Not met at original scope — propose REOPEN (blocked-external; tooling complete) |
| **T070** Record explicit human release decision | Each required owner signs the applicable release gates with evidence. / Agent reports remaining limits honestly and does not self-authorize public deployment. | honest limits reporting ready | this matrix | REQUIRED: multi-owner release decision with evidence | release decision | External approval — keep open |
| **T071** Build the requested public landing page | Preserve the user headline and cover all eight feature benefits in responsive layout. / Parent CTA, privacy/source routes and honest scope do not leak child-mode data. | landing page with required headline + 8 benefit cards + honest demo disclaimers | m1 e2e asserts headline; screenshots 5 widths | n/a | none | Met — keep closed |
| **T072** Implement parent starting-stage and session replacement controls | Only owning gated parent may change starting stage, unlock or replace an active session. / Overrides do not award completion/rewards and replaced session writes are denied. | stage-overrides + replace-session owner+gate scoped; no completion awarded by overrides | m2 tests; today's capture script exercised both via UI+API | n/a | replace-session has no parent-facing UI button yet (API-only) — minor gap noted | Met — keep closed (minor UI affordance noted) |
| **T073** Test replay, races and reporting invariants | Run concurrent session/answer/finish/profile-create tests against PostgreSQL. / Replay, version change, zero denominator, timezone and heartbeat cases retain correct results. | concurrency invariants | parallel finish/profile-create tests on real PostgreSQL | n/a | none | Met — keep closed |
| **T074** Complete empty/error/permission/recall UI states | Every data screen has tested loading, empty, denied, expired and recoverable-error behavior. / Child content recall/offline paths and parent privacy-job failures remain safe and understandable. | loading/empty/denied/expired/recall states across screens; recall UI shows safe return path; privacy journey states | e2e specs cover loading/empty/gate/recall error notes; permission denials via isolation tests | n/a | state-coverage is partial-automated (catalog empty/error states manual) | Met — keep closed with coverage note |
## Execution log

Checks actually executed during this reconciliation pass (local dev stack:
web :5181, API :3310, docker PostgreSQL `rzq-kids-db` :5433).

| Check | Command | Outcome | Evidence |
| --- | --- | --- | --- |
| Typecheck | `bun run typecheck` | PASS (clean) | — |
| Integration suite (incl. new 404-mapping, health-probe, rate-limit-budget, recall-surface tests) | `bun run test:integration` | PASS 13/13 | this session |
| Security: response shapes (new) | `bunx vitest run --project security tests/security/response-shapes.test.ts` | PASS 2/2 | this session |
| Recall surfaces (extended m3) | `bunx vitest run --project integration tests/api/m3-editorial-workflow.test.ts` | PASS (catalog hidden, lesson 404, session 404, media 503, events 410) | this session |
| Runtime outbound behavior (new e2e) | `bun run test:e2e -- tests/e2e/outbound-behavior.spec.ts --project=mobile-390` | PASS after removing Google Fonts (see below) | this session |
| Restore drill with independent ledger artifact | `bash scripts/restore-drill.sh` | PASS — resurrected profile re-deleted via ledger restored from standalone pg_dump | evidence/restore-drill-20260905-174920.md |
| Mockup/design delta | `bun scripts/capture-mockup-compare.ts` | 8 captures | evidence/mockup-compare/COMPARISON.md |

**Findings fixed during the pass** (defect fixes against already-closed criteria, not feature expansion):

1. Recall did not block new media authorization → `media_assets` now recalled with shared-asset protection (apps/api/src/modules/admin.ts) + tested.
2. Fonts were fetched from fonts.googleapis.com/gstatic.com in every child session → self-hosted under SIL OFL 1.1 with license texts (`apps/web/public/fonts/`); outbound e2e proves first-party-only traffic.
3. Gate rate limiter consumed budget on **successful** unlocks, locking out the designed parent↔child toggle flow → failures-only budget (rate-limit.ts, identity.ts) + regression tests.
4. Unknown API routes returned 500 → honest 404 with regression tests.
5. `SESSION_IN_USE` rendered a misleading "material unavailable" note → honest resume screen jumping to the first unfinished unit (lesson-player.tsx).

**Checks that could NOT be executed in this environment** (not failures — blocked):

- Production preflight/incident drills against a real release environment (T069): no external deployment is authorized.
- Off-site (independent-host) suppression-ledger copy: no second infrastructure exists; drill models it with a standalone artifact on the same host.
- WebKit/Safari Arabic-rendering verification (T058 boundary): Playwright matrix is Chromium-only.
- Human text/audio alignment recording (T045): impossible until licensed audio exists (T063).

**Gaps discovered and NOT fixed in this pass** (per the reconciliation prompt: not a
feature-expansion pass — all are implementable locally and await authorization):

- T012: password recovery flow absent (UI even references it from the gate page).
- T029: `/api/v1/media/stream/:id` handler absent; playback tokens are bare UUIDs without expiry signatures.
- T041: staff MFA ("current MFA evidence") not implemented.
- T055: explicit CSP response header and request body-size limit not implemented.
- T072 (minor): replace-session has no parent-facing UI affordance (API-only).

## Proposed issue changes

**Proposals only. None applied: the v1.1 package explicitly withholds authorization
("apply them only when authorized"). Awaiting product-owner approval.**

| Issue (task) | Current status | Proposed change | Reason |
| --- | --- | --- | --- |
| #73 (T069) | closed | **REOPEN** as blocked-ops; tooling sub-item stays evidenced in the closure comment | Closed against narrower scope: fail-closed preflight tooling is complete, but the original acceptance (run preflight + incident drills on the release environment, verify contacts, confirm no demo/unapproved publication) requires operational execution blocked on T063/T064/T065/T068 + real infrastructure |
| #47 (T045) | closed | **REOPEN** as blocked-external; record explicit mapping: human text/audio alignment checks per pilot segment are a sub-item of T064 reviewer sign-off | Criterion 1 (validation/hashes) evidenced; criterion 2 (recorded human alignment) has no evidence and cannot exist until licensed audio arrives (T063) |
| #43-adjacent (T041) | closed | **REOPEN** with engineering remainder: staff MFA gate | MFA criterion unimplemented; capabilities half is done and tested |
| (T012) | closed | **REOPEN** with engineering remainder: password recovery flow | Recovery flow referenced by UI but absent; expiry/resend-limit criteria untestable until it exists |
| (T029) | closed | **REOPEN** with engineering remainder: media stream route + short-lived signed tokens | Authorization half done; delivery half absent (mitigated: zero licensed audio) |
| (T055) | closed | **REOPEN** with small remainder: CSP header + body-size limit (+ tests) | Origin/rate-limit/cookie parts verified; CSP/body-size parts unimplemented |
| #72 (T068) | open | Keep open; **reclassify label/note**: blocked engineering/QA dependent on pilot findings — not a human-only approval | Per acceptance review §2 |
| #68 (T064) | open | Keep open; **add explicit sub-item**: T045 human text/audio alignment per pilot segment against exact release hashes | Makes the T045→T064 ownership mapping visible |
| #67, #69, #70, #71 (T063, T065, T066, T067) | open | Keep open (external gates) | Unchanged |
| #74 (T070) | open | Keep open | Unchanged |

If approved, net effect: 61 closed / 13 open (6 reopened with scoped engineering
remainders + 7 existing external gates) — an honest count replacing a flattering one.

## Milestone summary

- **Implemented and inspected:** M0–M4 engineering complete and evidence-mapped (this
  matrix); five defect fixes applied and tested during reconciliation; four scoped
  engineering remainders discovered (T012/T029/T041/T055) and documented, not built.
- **Acceptance tested with recorded evidence:** 74/74 tasks now have per-criterion
  evidence classifications; recall enforcement, runtime response shapes, first-party
  outbound behavior, and independent-ledger restore replay strengthened with new
  passing tests/drills this pass.
- **External approvals actually inspected:** none exist; all remain pending
  (T063–T067, T070) — never fabricated.
- **Remaining blocked engineering/operations:** T012/T029/T041/T055 remainders
  (local-runnable); T068 regression run (pilot-dependent); T069 operational drills
  (release environment).
- **Release decision:** NOT authorized. Production stays fail-closed; demo mode,
  demo content, and enrollment gates remain in force.
